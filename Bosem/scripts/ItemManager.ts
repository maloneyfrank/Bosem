module Bosem {
    export class ItemManager {
        static ON_TIME_INTERVAL: number = 0;
        static init( type: number) {
            switch type{
                case this.ON_TIME_INTERVAL:
                    break;
            }
        }
        static onTimeInterval() {
            
        }
    }
}